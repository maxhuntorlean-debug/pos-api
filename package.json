{
  "name": "pos-api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "migrate": "wrangler d1 migrations apply pos-api-db --remote",
    "deploy": "npm run migrate && wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}